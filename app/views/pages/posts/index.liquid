{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id

  function main_group = 'lib/queries/groups/find', name: 'main'
  function posts = 'lib/queries/posts/search', limit: 100, parent_id_exists: false, group_ids: main_group.id, tags: context.params.tag

  include 'theme/simple/posts/index', posts: posts
%}
