{% liquid
  assign statuses = 'published,unavailable' | split: ','
  function items = 'lib/queries/items/search', promoted: true, limit: 6, statuses: statuses, lang: context.language, only_public: true

  return items
%}
