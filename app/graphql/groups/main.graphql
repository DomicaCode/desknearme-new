query search(
  $name: String!
) {
  groups: records(
    per_page: 1
    filter: {
      table: { value: "group" }
      properties: [{ name: "name", value: $name }]
    }
  ) {
    total_entries
    results {
      id
      type: table
    }
  }
}
